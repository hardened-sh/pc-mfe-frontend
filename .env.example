# Server Configuration
PORT=3000
NODE_ENV=development

# Security
# IMPORTANTE: Em produção, use secrets gerados com: openssl rand -base64 32
CRYPTO_SECRET=mfe-poc-demo-secret-2026-hardened
SESSION_SECRET=mfe-poc-session-secret-2026-hardened

# Logging (debug, info, warn, error)
LOG_LEVEL=info

# POC Mode (true = força modo vulnerável no início)
VULNERABLE=false
